#! /bin/sh -e

case "$1" in
    configure)
	base=@PKGBASE@
	docdir=/usr/share/doc/@PKGBASE@2
	if [ -d $docdir ] && [ ! -h $docdir ]; then
	    rm -rf $docdir
	    ln -s @PKGBASE@ $docdir
	fi
esac

#DEBHELPER#
