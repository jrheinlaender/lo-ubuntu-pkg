#!/bin/sh

set -e

#INCLUDE_SHELL_LIB#

case "$1" in
	upgrade)
		if dpkg --compare-versions `echo $2 | cut -d+ -f2` lt OOo3.3.0~; then
			remove_extension com.sun.reportdesigner
		fi
	;;
esac

#DEBHELPER#

exit 0
