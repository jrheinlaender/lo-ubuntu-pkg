#!/bin/sh

# postinst script for libuno-cil
THIS_PACKAGE=libuno-cil
THIS_SCRIPT=postinst

for dll in cli_basetypes.dll cli_cppuhelper.dll cli_types.dll cli_uno_bridges.dll cli_ure.dll; do
	gacutil -i /usr/lib/openoffice/program/$dll -package $THIS_PACKAGE || true
done

#DEBHELPER#

exit 0
