#!/bin/sh

# prerm script for libuno-cil
THIS_PACKAGE=libuno-cil
THIS_SCRIPT=prerm

for dll in cli_basetypes.dll cli_cppuhelper.dll cli_types.dll cli_uno_bridges.dll cli_ure.dll; do
	gacutil -u `basename $dll` -package openoffice || true
done

#DEBHELPER#

exit 0
