#!/bin/sh

# postrm script for openoffice.org-core

THIS_PACKAGE=openoffice.org-core
THIS_SCRIPT=postrm

set -e

#INCLUDE_SHELL_LIB#

if [ "$1" = "remove" ]; then
	rm -rf /var/spool/openoffice$VER
fi

#DEBHELPER#

exit 0
