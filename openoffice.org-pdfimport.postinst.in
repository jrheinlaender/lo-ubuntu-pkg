#!/bin/sh

set -e

THIS_PACKAGE=openoffice.org-pdfimport
THIS_SCRIPT=postinst

#INCLUDE_SHELL_LIB#

if [ "$1" = "configure" -o "$1" = "upgrade" ]; then
	add_extension /usr/lib/openoffice/share/extension/install/pdfimport.oxt
fi

#DEBHELPER#

exit 0
