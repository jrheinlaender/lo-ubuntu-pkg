#!/bin/sh

set -e

THIS_PACKAGE=openoffice.org-oresentation-minimizer
THIS_SCRIPT=postinst

#INCLUDE_SHELL_LIB#

if [ "$1" = "configure" -o "$1" = "upgrade" ]; then
	add_extension /usr/lib/openoffice/share/extension/install/sun-presentation-minimizer.oxt
fi

#DEBHELPER#

exit 0
