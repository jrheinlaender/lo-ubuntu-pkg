#!/bin/sh

set -e

# preinst script for openoffice.org-sdbc-postgresqö

THIS_PACKAGE=openoffice.org-sdbc-postgresql
THIS_SCRIPT=prerm

#INCLUDE_SHELL_LIB#

case "$1" in
	remove)
                id=`/usr/lib/openoffice/program/unopkg list --shared | grep Identifier:.*postgresql-sdbc | cut -d":" -f2`
                remove_extension $id
	;;
esac

#DEBHELPER#

exit 0
