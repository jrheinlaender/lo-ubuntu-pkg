#!/bin/sh

set -e

THIS_PACKAGE=openoffice.org-wiki-publisher
THIS_SCRIPT=postinst

#INCLUDE_SHELL_LIB#

if [ "$1" = "configure" -o "$1" = "upgrade" ]; then
	add_extension /usr/lib/openoffice/share/extension/install/wiki-publisher.oxt
fi

#DEBHELPER#

exit 0
