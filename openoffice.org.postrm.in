#!/bin/sh
# postrm script for openoffice.org

THIS_PACKGAGE=openoffice.org
THIS_SCRIPT=postrm

#INCLUDE_SHELL_LIB#

set -e

if [ "$1" = "remove" ]; then
	# if this still exists and is empty, remove it...
	rmdir /usr/lib/openoffice 2>/dev/null || true
fi

exit 0
