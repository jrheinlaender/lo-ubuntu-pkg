#!/bin/sh

# postrm script for openoffice.org2-jars-native

THIS_PACKAGE=openoffice.org2-jars-native
THIS_SCRIPT=postrm

set -e

if [ "$1" = "remove" ]; then
	rm -f /var/lib/openoffice2/openoffice.org.gcjdb
	rmdir /var/lib/openoffice2
fi

exit 0
