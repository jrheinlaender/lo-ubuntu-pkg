Index: oo_cvs/instsetoo/util/makefile.mk
===================================================================
RCS file: /cvs/installation/instsetoo/util/makefile.mk,v
retrieving revision 1.12.2.1
diff -u -3 -p -r1.12.2.1 makefile.mk
--- oo_cvs/instsetoo/util/makefile.mk	4 Jul 2003 16:44:05 -0000	1.12.2.1
+++ oo_cvs/instsetoo/util/makefile.mk	14 Jul 2003 13:30:36 -0000
@@ -99,12 +99,17 @@ SEARCH_DIR=.
 
 ALLTAR : pack
 
-.IF "$(alllangext)"!=""
+# Create installation sets for languages in instsetext, or in alllangext if
+# insetsetext is not defined in the format instsetext=<num>,[<num>,]...
+# e.g.: instsetext=01,49,99
+instsetext*=$(alllangext)
+  
+.IF "$(instsetext)"!=""
 
 .IF "$(BUILD_SPECIAL)"!=""
 pack: pack_01 
 .ELSE
-pack: $(foreach,i,$(alllangext) pack_$i)
+pack: $(foreach,i,$(instsetext) pack_$i)
 .ENDIF
 
 pack_%:
@@ -115,10 +120,12 @@ pack_%:
 test:
 	+-$(LZIP) $(LZIPFLAGS) -p ${SEARCH_DIR} -p ${SHARED_COM_SDK_PATH} -l 01 -f openoffice.lst -o -n OfficeOSL 
 
-.ELSE			# "$(alllangext)"!=""
+.ELSE			# "$(instsetext)"!=""
 pack:
-	@+echo cannot pack nothing...
+	@+echo no installation sets selected to be packed.
+	@+echo instsetext: $(instsetext)
+	@+echo alllangext: $(alllangext)
 
-.ENDIF			# "$(alllangext)"!=""
+.ENDIF			# "$(instsetext)"!=""
 
