# This file is NOT a shell script.
#
# This file gets included by both debian/rules (make) AND the scripts in
# debian/scripts (Bourne shell).
ENVFILE = LinuxPPCEnv.Set.sh
ARCHBUILDDIR=unxlngppc.pro

#depends
#DEPS="libstdc++4, libc6 (>=2.2.5-12.1), libstlport4.5gcc3.1 (>=4.5.3-5cjh2)"

# precompiled mozlibs are not available, and we haven't got a package for this yet
MOZILLA=n
