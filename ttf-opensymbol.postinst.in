#!/bin/sh
# postinst script for ttf-opensymbol

THIS_PACKGAGE=ttf-opensymbol
THIS_SCRIPT=postinst

#INCLUDE_SHELL_LIB#

set -e

if [ "$1" = "configure" ] && [ -x /usr/bin/fc-cache ]; then
	echo "Updating fontconfig cache..."
	fc-cache -f
fi

exit 0
